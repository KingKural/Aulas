let firstInput = "";

function calcButton1() {
    firstInput = document.getElementById("calculatorDisplay");
    let number = 1;
    if (firstInput.textContent == 0) { firstInput.textContent = number; }
    else { firstInput.textContent += number; }

    return firstInput;
}
function calcButton2() {
    let firstInput = document.getElementById("calculatorDisplay");
    let number = 2;
    if (firstInput.textContent == 0) { firstInput.textContent = number; }
    else { firstInput.textContent += number; }

    return firstInput;
}
function calcButton3() {
    let firstInput = document.getElementById("calculatorDisplay");
    let number = 3;
    if (firstInput.textContent == 0) { firstInput.textContent = number; }
    else { firstInput.textContent += number; }

    return firstInput;
}
function calcButton4() {
    let firstInput = document.getElementById("calculatorDisplay");
    let number = 4;
    if (firstInput.textContent == 0) { firstInput.textContent = number; }
    else { firstInput.textContent += number; }

    return firstInput;
}
function calcButton5() {
    let firstInput = document.getElementById("calculatorDisplay");
    let number = 5;
    if (firstInput.textContent == 0) { firstInput.textContent = number; }
    else { firstInput.textContent += number; }

    return firstInput;
}
function calcButton6() {
    let firstInput = document.getElementById("calculatorDisplay");
    let number = 6;
    if (firstInput.textContent == 0) { firstInput.textContent = number; }
    else { firstInput.textContent += number; }

    return firstInput;
}
function calcButton7() {
    let firstInput = document.getElementById("calculatorDisplay");
    let number = 7;
    if (firstInput.textContent == 0) { firstInput.textContent = number; }
    else { firstInput.textContent += number; }

    return firstInput;
}
function calcButton8() {
    let firstInput = document.getElementById("calculatorDisplay");
    let number = 8;
    if (firstInput.textContent == 0) { firstInput.textContent = number; }
    else { firstInput.textContent += number; }

    return firstInput;
}
function calcButton9() {
    let firstInput = document.getElementById("calculatorDisplay");
    let number = 9;
    if (firstInput.textContent == 0) { firstInput.textContent = number; }
    else { firstInput.textContent += number; }

    return firstInput;
}
function calcButton0() {
    let firstInput = document.getElementById("calculatorDisplay");
    let number = 0;
    if (firstInput.textContent == 0) { firstInput.textContent = number; }
    else { firstInput.textContent += number; }

    return firstInput;
}
function calcButtonClear() {
    let firstInput = document.getElementById("calculatorDisplay");
     return firstInput.textContent = 0;
}